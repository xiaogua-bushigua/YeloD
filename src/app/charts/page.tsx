import LayoutWrapper from '@/components/LayoutWrapper';

export const metadata = {
	title: 'Charts',
	description: '我是管理',
};

export default function Charts() {
	return <LayoutWrapper>charts</LayoutWrapper>;
}
