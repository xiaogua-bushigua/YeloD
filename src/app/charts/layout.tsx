
export const metadata = {
	title: 'Charts',
	description: 'charts page',
};

const ChartsLayout = ({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) => {
	return <>{children}</>;
};

export default ChartsLayout;
