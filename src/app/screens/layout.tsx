export const metadata = {
	title: 'Screens',
	description: 'screens page',
};

const ScreensLayout = ({
	children,
}: Readonly<{
	children: React.ReactNode;
}>) => {
	return <>{children}</>;
};

export default ScreensLayout;
